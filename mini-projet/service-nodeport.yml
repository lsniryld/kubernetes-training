apiVersion: v1
kind: Service
metadata:
  name: my-svc
  namespace: tp-wordpress
spec:
  type: NodePort
  selector:
    name: wordpress
  ports:
    - targetPort: 80
      port: 80
      nodePort: 30007
